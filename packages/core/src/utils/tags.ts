/**
 * Utility functions for managing comma-separated tags (categories, resources, etc.)
 * Re-exports from the generic comma-separated utility for backwards compatibility
 */
export {
	addTag,
	getLastTag,
	hasTag,
	parseList,
	parseTags,
	removeTag,
	stringifyList,
	stringifyTags,
} from "./comma-separated";
