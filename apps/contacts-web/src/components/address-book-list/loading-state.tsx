/**
 * Loading state component for address books list
 * Wrapper around shared ListLoadingState component
 */

import { ListLoadingState } from "@appstandard/ui";

export function AddressBooksListLoadingState() {
	return <ListLoadingState />;
}
